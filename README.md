# hello-react

This workspace contains a small Vite + React starter created in Codespaces.

Run locally:

```bash
npm install
npm run dev
```

Open `http://localhost:5173` in your browser after running the dev server.

Files to edit:
- [src/App.jsx](src/App.jsx) — main app component
